# scratch/verification artifacts (but assets/ and ref/ PNGs are real files)
*.png
!assets/**/*.png
icons.json

# v4 scaffold
node_modules
diff/
.env

# ref/ holds Figma ground-truth exports — keep these despite *.png above
!ref/*.png

# stray design-app lock files
*.idlk

# any env file, wherever it is named
*.env
.env
