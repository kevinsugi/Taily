# Taily v4 prototype

Visual source of truth: Figma `spK8ZHnsPlWEuD8zRmf5sY`, page "User - Main Flow 2" (`277:2636`). Components page `183:2293`.
Screens (id → node): see `scripts/screens.json`. Behaviour source of truth: `js/state.js` (ported from v3, do not redesign).

## Numbering (Phase R7 — Kevin's scheme, applied to Figma frames + code ids)

Number = journey stage (01 Home, 02 Details, 03 Order Status, 04 Review, 05 Handoff, 06 Complete, 09/10 tabs). Letter suffix = sibling screen (05A/05B). Dot suffix = an overlay owned by that screen (02.1, 03.1). **03 - Order Status is ONE screen family, variants named by status** (Requested / Confirmed / Reminder / Tailoring / Ready* / Summary / Cancelled — *Ready is a live state of the Tailoring frame, no frame yet). Old → new (phase-history prose below still uses the OLD short names): 03→03/Requested · 04C→03/Confirmed · 05→03/Reminder · 04D→03/Tailoring · 04E→03/Summary · 05X→03/Cancelled · R1→03.1 · 05C→03.2 · PV3→03.3 · 06A/06B→04 Default/Modified · RC1→04.1 · 07→05 · 07A/07B→05A/05B · 07C→05.1 · 08→06 · 08C→06.1 · 02A/02B/04A/04B→02.1–02.4 · M1→10. The canvas is organised in rows by stage; the flow-map board sits above the grid.
Phase R0: screen 06 (Order Status) was deleted from Figma — 04D, renamed "Appointment Status" (same node `308:3578`, still keyed `03-status-tailoring`), takes its place; 05's confirm and the old timeline→review affordance now route 05 → 04D → (tap order) → 06B. `04-review-approve-modified` (551:6263) is 06A with the order modified at the appointment (added service + garment in semantic/info).
Phase R2 screens: `03.1-reschedule-popup` (558:1867, modal over 05 — opened from 04C's / 05's Reschedule / Cancel AND the 01/09 card Reschedule and 03's cancel line; confirming runs `cancelAppointment()` → 05X), `03-status-cancelled` (558:3817, error-red heading), `03.3-photo-viewer` (559:2112, filmstrip over 04D — opened by tapping any Before/Pinned photo row via `wirePhotoViewer()`; closes INSTANTLY via modalOverlay's `instant` option; pre-appointment ViewOnly tiles are deliberately inert), `04.1-request-changes` (559:3265, modal over 05 per the frame — opened from Request Changes on 06A/06B; Sounds Good dismisses). Live popups mount through `modalOverlay()` (components.js); the registered routes render the frame-verbatim backdrop (`viewReminder` / `viewTailoring`) for the diff harness. The popup frames' backdrops were re-synced via the Figma API in Phase R4 (Jul 12 rows; PV3 carries a clone of 04D's Active Job Card) — re-sync them again whenever 05/04D change.
Phase R4: `03.2-appointment-confirmed` (570:8329, success-green modal over 05 — 05's Confirm Appointment opens it; its Confirm runs `completeAppointment()` → 04D). 04D's hero is the user-side Active Job Card (`.status-card`); modal glyph rows are 14-glyph/16-copy.
Phase R6: `05.1-window-confirmed` (576:9219 / Window Confirmed Modal 580:2230 — 07A/07B's confirm runs `chooseFulfilment()` and opens it over the live screen; Done → Home; the order STAYS ready) and `06.1-leave-review` (584:9430 / Leave Review Sheet — 08's Leave a Review opens it; stars select 1–5 live, real textarea, Confirm closes with a toast; route renders the 4-star fixture). **08 is gated on the TAILOR confirming the handoff**: a scheduled-ready card opens 04D (note shows the window), and tapping the order there is the demo tailor-confirms (`deliver()`) → 08. `--size-30` tokenised (stars, not a Figma variable); 08C's 1.31 baseline is ★-glyph font metrics.
Phase R5: the Active Job Card is a real COMPONENT ("User - Active Job Card" 575:2154, Components page) with linked instances in 04D and PV3's backdrop — they stay in sync. 04D's status card mirrors the ORDER's real status (pill + progress mapped from `canonicalStatus`; the harness's 'confirmed' renders the frame's Tailoring fixture). **07 is gated on ready**: 05 → 05C → 04D (awaiting-approval, order tap → 06B) → approve → 04D (tailoring; order tap = DEMO tailor-finishes → `markReady()`, stays on 04D) → the Ready appointment card's "Schedule Pickup / Delivery" (01/09, wired) or 04D's order tap → 07. The Appointment Card gained Status=Requested in Figma (571:9099, renamed from Status5 via API): warning pill, confirmed-style bar, "We will match you…" note (`.appt-card__note`), no actions — and clicking a Requested card does nothing (`apptTarget` → null). Card helpers normalise v4 chain names (`cardStatus`). Mouse click-drag pans every screen vertically (`wireDragScroll` in app.js; photo rows/wheels opt out).
Phase R8: **every 03 variant (and 05) opens with the `statusHero` chassis** — pill matching the status + title (+ body/rows where the frame draws them): Requested `requested`/"Finding your tailor…" (hero now ABOVE the map — the old 12px inner pad is gone), Confirmed `confirmed` ink title, Reminder `confirmed`, Tailoring `tailoring`/"Tailoring in Progress.", Summary `completed` + Items Received row, Cancelled `declined` error-red title, 05 `ready` success title. 03/Tailoring **dropped the Active Job Card hero** for pill + hero + `summaryCard` (Appt/Need-by rows read the live appointment; the frame's abbreviated fixture is ALLOW'd) — the "User - Active Job Card" component master stays on the Components page but no user-flow frame instances remain. The reminder's and popups' "Order Summary" heading and 06/Summary's `#TLY-2026-4417` line are gone. **Appointment cards always open their respective 03 variant** (`apptTarget`): requested→03/Requested, confirmed→03/Confirmed (new), awaiting-approval/tailoring/ready→03/Tailoring, completed→03/Summary; the Ready card's Schedule Pickup / Delivery CTA still goes to 05. Backdrops re-synced via API: 03.1/03.2/04.1 carry the new reminder body, 06.1 the new 06 body, PV3 the new Tailoring layout.

## Rules

- Match Figma exactly: px, weights, colors, line-heights from `get_design_context`. Never round, "improve", or re-space.
- Colors, spacing, radius, type sizes ONLY through `css/tokens.css` variables. No literal hex/px for those outside `tokens.css`.
- Type comes from the Figma type scale — `size/10, 12, 16, 24, 28, 32, 36`, every text node bound to it. 12 / 16 / 28 carry most of the UI; hierarchy via **weight + color**, never by inventing a size. Families: `family/serif` = Cormorant Garamond (headings), `family/sans` = Hanken Grotesk (everything else).
- Components live in `css/components.css` + `js/components.js`, one class per Figma variant. Add a variant there before using it in a screen. Never fork a component inside a screen.
- Screens: 390 wide, background `var(--neutral-50)`; `.status-bar` absolute 0/0 h44; `.top-nav` absolute 0/44 h56; `.body` padding-top **128**, sides **20**.
- If Figma looks wrong or inconsistent, STOP and report it; do not fix it silently.

Workflow per round (Phase R): `npm run refs` → `npm run refs:check` → code → `npm run diff -- <id> --accept` for intended screens → `npm run check` → commit. (New screens still start from `get_design_context` → `js/screens/<id>.js`.) Diff gates are per-screen ratchet baselines in `scripts/screens.json` (pass = baseline + 0.1); `npm run check` = diffs + text parity + click-through + style hygiene in one command. `proto-v1` tags the pre-refinement ground truth.

`ref/` PNGs are Figma exports @2x (`npm run refs`, needs `FIGMA_TOKEN`). Re-export if Figma changed.

No build step. Plain HTML/CSS/JS. Deploys to GitHub Pages from `main`.

## Verified in Phase 0

All 17 frames were normalised before any code was written. These are measured, not assumed:

- **Content offset is 128 on every screen.** Status Bar and Top Nav are absolute instances at `(0,0) 390×44` and `(0,44) 390×56`. Side padding 20 (content width 350).
- **Every color is bound to a `Taily/Colors` variable.** No literal fills remain, so `tokens.css` can be generated straight from the variables.
- **All spacing is on the 4px grid** and bound to `space/4–40`, except the 128 top offset and one 48 (no variable exists at those values).
- **CTA is 54px tall** in both variants (Default and Secondary) and 350 wide, across all 26 instances.
- Sheet scrims are `ink @ 0.4` node opacity over the app background — not a flat grey.

Components added in Phase 0, on the Components page:

- `Time Chip` `501:1039` — `State=Default | Selected`, 94×45, r12. Used by 07A / 07B.
- `Select Time` `502:1035` — label + chevron trigger, 129×37, r8. Used by 07A / 07B.

## Type API (css/base.css)

Nothing outside `base.css` sets `font-size`. Compose with a type class + a weight modifier + a colour utility.

| class | Figma |
|---|---|
| `.t-title` | Cormorant **Medium** 28 / 34px — page titles. `.w-600` for the SemiBold ones. |
| `.t-section` | Cormorant SemiBold 20 / 24px — section headers (Upcoming Appointments) and the 02a sheet title (`.sheet__title`). **20 is off the type scale** — see inconsistencies below. |
| `.t-body` | Hanken 16 / 21px, Regular |
| `.t-small` | Hanken 12 / 16px, Medium |
| `.t-caps` | Hanken 12 / 16px SemiBold, uppercase, +1px tracking |
| `.t-micro` | Hanken 10 / 13px SemiBold |

Weights: `.w-400 .w-500 .w-600 .w-700`. Colours: `.c-ink .c-700 .c-600 .c-500 .c-400 .c-300 .c-white .c-accent .c-accent-ink .c-sage .c-success .c-warning .c-error .c-info`.

Line-heights are the px Figma actually renders — `lineHeight` is AUTO on nearly every node, resolving to ≈1.31 for Hanken and ≈1.21 for Cormorant, rounded to whole px. Do not write `normal`.

Chrome lives in `js/components.js`: `statusBar(time)`, `topNav(active)`, `chrome(active, time)`.

## Component library (Phase 3)

`css/components.css` + `js/components.js` (27 render fns) + `components.html` gallery. One class per Figma variant. Icons are exact Figma vectors in `js/icons.js` (generated from `assets/icons/`, currentColor). Figma strokes are INSIDE — bordered hug-height components use `box-shadow: inset 0 0 0 1px …`, never `border`, or they grow 2px. Key fns: `cta, ctaSmall, statusPill (9), timeChip, selectTime, filterPill, garmentTile, apptCard, statusHero (6), summaryCard, garmentCard (4), progressBar (4), bubble, timeline, deliveryWindow, infoCard/infoRow/metaRow/feeRow, methodRow, sheet, sheetOverlay, wheel, wheelScroll/wireWheel, selector`. `Selector` `535:1582` (Aug 2026) is the garment-card inline dropdown — Type=Quantity/Item/Job/**Additional** × State=Closed/Open(/Selected); menu absolute (96/220 wide) so the trigger footprint never changes. `additionalSelector()` renders the ⊕ trigger, the priced ADD A SERVICE menu, and added-service rows with ✕ remove. `02.2-address-sheet` (538:1382) is the structured address form — `openAddressOverlay()` opens it from the 01/02 heading address line; Save writes `state.contact` and updates the opener's DOM in place. Motion: press scale + sheet drawer per the animate skill; `prefers-reduced-motion` handled.

Phase R0 additions: `feeRow(price, desc, {line, info})` — `line` draws the inter-row hairline (inset, every row but the last), `info` paints the price semantic/info. `garmentCard` gained `added` (whole card info — 06B's added garment), `priceInfo` (just the price), and services entries may be `{label, added}` (06B's added service). View-variant service text is Medium 16 accent-ink (was unstyled/ink until R0 — the fix dropped every summary screen's diff). One exception to the strokes-INSIDE rule: the Filter Pill box master now includes its 1px stroke in layout (39 tall), so `.filter-pill__box` uses a real `border`.
Phase R2 additions: `modalOverlay()` + `.modal` classes (R1/RC1 — serif-24 title, 14/18 glyph rows, ✂ badge; scrims at ink@45% need `z-index: 3` to clear the z2 chrome). `.photo-viewer` (PV3 filmstrip; scrim ink@95%, glyphs use the not-a-Figma-variable `--size-18`). `.photo-row` (Before/Pinned) scrolls horizontally per Kevin — scrollbar hidden, clipped at the card's padding edge like the frames — and tapping it opens PV3 on post-appointment screens only.

## Deliberate deviations from the frames (Kevin-directed)

- **03's map is a placeholder** (`.map-card--placeholder`, media-placeholder fill + note) — it will become a live Google Map centred on the user's location. The frame shows a map raster, so `npm run diff -- 03-status-requested` reads ~1.4% and is expected to exceed the 1% gate until the real map lands.
- **03's request card reads the live order (Phase R3)** — address/visit, requested time, and `${n} items · $X.00+ est. · $Y deposit held` come from state so they match whatever was booked; the frame's fixture (Thu, Jul 9 · 9:30 AM, Home Visit, 2 items $200/$20) is stale and ALLOW'd in text parity. Cancel request opens the R1 popup.
- **07A/07B are interactive (Phase R3)** — chips select (shared `state.ui.window`, DEFAULT = the first option), the confirm CTA follows the selection, Request Custom Time opens the 02A wheel (`openDateTimeOverlay('custom', onSet)`), and the secondary CTA cross-navigates (07A "Select Delivery" → 07B, 07B "Select Pickup" → 07A). The frames still draw Fri 4–6 PM selected with same-screen secondary labels — baselines sit at ~2.5% until the frames are updated.
- **Both 02 pills store "Jul 12, 7:00 PM" format (Phase R3)** — the 02A picker writes `Mon D, H:MM AM` for Requested time AND Need by (v3's day-only needBy is gone).

## Known Figma inconsistencies — do not silently fix

- **Top Nav `Active` state**: `04c` / `04d` use `Active=Bookings`, but the "body" screens (03, 05, M1, 06a, 06b, 07, 07a, 07b, 08) use `Active=Home` even though they are post-booking. Build what Figma shows; raise it rather than correcting it.
- ~~The modified-order math on 04D/04E~~ — resolved in Phase R1: the fee fiction is now consistent everywhere (Subtotal `$360` − `$20` deposit = `$340` due; + `$20` delivery = `$360` paid). The cards still itemize `$120/$80/$80` (= $280) only on 04D/04E/08 — the +$80 added service is visible only on 06B's card 1.
- **The Jul 12 fiction (Phase R1/R2)** — appointment `Sunday Jul 12, 7PM`; need-by `Friday Jul 17`; deposit 7/7; confirmed 7/12; delivery/pickup Fri 4-6PM Jul 17. All frames synced (Kevin resolved the R1 quirks: 07A/07B CTAs now `· Fri 4-6PM`, James badge `JUL 1`, Need By `Friday`). 01's two item lines under "3 Items Total" are **intentional** — 01 shows the appointment before it happens (the third garment is added at the appointment). Remaining fiction quirks, carried verbatim: R1's modal row says `Thursday's 7:00 PM with Marco is cancelled`; PV3's sub reads `Pinned by Marco · Thu, Jul 17` (pinning happened Jul 12); RC1's frame draws 05 as its backdrop though it opens from 06A/06B.
- ~~02B's frame backdrop is stale~~ — resolved in Phase R1: the frame was re-synced to the new 02 (diff 0.05).
- **Fractional CTA heights**: the first CTA instance on 05 (491:836) and 08 (491:854) is 53.667px tall where the master is 54 — frames 05/08 end on .67 heights. Built at 54. Raised.
- **The deposit row always reads `-$20` (Kevin, Phase R0.1)** — the 04D/04E/06A/06B/08 frames still write it spaced (`- $20`); built unspaced everywhere (text-parity ALLOW covers the spaced frame nodes, PV3's 04D backdrop included).
- `apple-pay-badge` / `google-pay-badge` on `04a` are plain frames by decision (brand marks), not components.
- `09-bookings` is a top-level tab, not a step in the booking flow; its number is a filing convention only.
- `T2 / Chat Bubble` text moved to 16px in refine r1 (`.bubble` uses `--size-16`); `size/14` remains a real Figma variable used by `.selector__option` and 02B's sub/toggle copy. Photo-tile gradient starts at `#E2DCD1`, one digit off `neutral-200`. The 02a sheet draws a 40×4 grabber where 04a/04b use 36×4. Both still await Kevin's call.
- ~~02's fee fiction is stale~~ — resolved in UX-LOOP round 1: the 02 frame (+ its four sheet backdrops) now seeds $120 Hem + $80 Sleeve, so the honest deposit is the frame's $20. The Job master's menu also lists short names (Taper / Sleeve / Resize / Repair) where the build's job menu uses the real `JOB_TYPES` keys.
- **Additional-service prices**: the Selector `Type=Additional` menu (541:1975) prices "Taper +$24" while `JOB_TYPES` has 'Taper / Slim Fit' at $90. Built per the frame — `data.js` gained short-name entries Taper $24 / Sleeve $80 / Lining $45 (`ADD_SERVICES` holds the menu order). Raised.
- ~~04b's backdrop is stale~~ — resolved in Phase R0: the 02a/04a/04b frames were re-synced to the new 02 backdrop (diffs now 0.23/0.64/0.42). Only 02B's backdrop remains stale (see above).
- **02B's scrim is ink@0.6** (538:1400) where every other sheet uses 0.4. Built as drawn (scoped override); raised.
- The Confirmed pill on 01's appointment card draws its ring at **full `--info` strength** where the Status Pill master specifies the text colour at 35% — instance vs master disagree. The 03 meta rows use **text glyphs (◉ ▤ ✂)** whose scissors renders upright in Figma's font but left-facing in Segoe. Both raised, not silently fixed.
- **Squished Status Pill instances (Phase R8)**: the new hero groups in 03/Tailoring (85×21.25), 03/Cancelled (86×21.5) and 03/Summary (98×24.5) carry SCALED pill instances where the master (and the Confirmed/Reminder/05 frames) is ×24 tall — clearly accidental group-scale. Built at the master's 24; the 2.5–2.75px downstream shift is why those three baselines sit at 3.29 / 3.30 / 0.74 (with ±0.5–2.5px height Δ). Raised — once Kevin restores the pills to 24, re-export and re-accept to drop the baselines back.
- The Aug 2026 serif headers — `Upcoming Header` (`532:958` on 01/01a) and the 02a sheet title (`277:2921`) — are **literal 20px, unbound to any size variable** (no `size/20` exists). Built verbatim via `--size-20` in `tokens.css` (marked as not-a-Figma-variable) + `.t-section` / `.sheet__title`. Raised, awaiting Kevin's call.
- The updated sheet frames (02a/04a/04b) draw the full 02 screen dimmed behind the scrim (was a flat backdrop) — built via `view02()` in `.sheet-backdrop`. 02a's picker sheet (358 tall at y490) and 04b's sheet (286 at y559) overflow the 844 frame by 4px / 1px; reproduced with negative `bottom` offsets, not padding changes.

## Tailor flow (Phase T — built Sep 9 2026, "Lovable-fast", refine later)

Page **"Tailor - Main Flow 2"** (`445:1491`) was designed by Kevin ahead of the T0 prompt (which is stale — do NOT create the page). Sep 1 2026: audited Phase 0-style and reconciled to the user-flow canon per Kevin's calls. Screens (all in `scripts/screens.json`, `t`-prefixed ids):

`t01-home 455:3560 · t02-appointment-request 455:2170 · t03-request-accepted 449:714 · t03a-decline-request 449:733 · t03b-job-cancelled 449:752 · t04-appointment-details 455:2587 · t05-confirm-final-pricing 449:809 · t06-appointment-status 473:6324 · t07-job-ready 449:847 · t08-job-complete 449:866`. The tailor's messages screen is the shared `10-messages`: with `state.persona === 'tailor'` it renders Sarah in the header and the same thread from Marco's side. Its frame is `TM1 - Message Customer` (570:8782, edited into the tailor view in UX-LOOP round 3) keyed `t10-messages`.

**Build (Phase T, one session, no worktree — Kevin chose speed over the T1–T4 ceremony):** tailor-only files are `js/tailor-data.js` (Sarah/Leo fiction, `jobView()` = payout/pill/stage per status, `jobTarget()` = which screen a job opens), `js/tailor-components.js` (`tailorChrome`, `requestCard`, `jobCard`, `tailorGarmentCard` Appt/Appt_View, T2 rows, `orderDropdown`), `css/tailor.css`, `js/screens/t*.js`, `ref/t*.png`, `scripts/clickthrough-tailor.mjs` (in `npm run check`). Shared touches: `topNav(active, items)` gained an optional items list (Home/Calendar/Shop); `app.js` lists the t-modules and owns the **persona gate** — `state.persona` flips to `tailor` when a `t0*` screen renders, back to `user` for any non-messages screen; the "View as Tailor / View as Customer" toggle sits in `.stage-caption` OUTSIDE `.screen` (diffs never see it). Tailor class names must not collide with user CSS (`.detail-row` did — now `.t-detail-*`).

**Mirror model:** the tailor reads `state.upcoming[0]` — the same appointment the user books. T02 Accept → `tailorAccepts()`; T03A Decline → `declineAppointment()`; T05 Send → `completeAppointment()` (user sees 03/Tailoring awaiting-approval + 04/Modified); T06 Mark Ready → `markReady()` (demo: if Sarah hasn't approved yet it runs `approveOrder()` first with a toast); T07 Mark Picked Up → `deliver()`. T01 keeps the request card until the tailor accepts/declines (`state.tailorUi.requestHandled`) — the frame shows the request AND the confirmed job. Deep-linked T06 with the seed's `confirmed` renders the frame's Tailoring fixture (like 04D). Fast-build baselines are wide (T02 8%, T05/T06 ~8.5%, T03 6%, T08 4%, T04 4.75%, T03B 4.9%; T01/T07 <1%) — the refinement rounds ratchet them down; the editable selectors on T04 are inert (toast).

- **It is Marco's view of the user flow's one appointment** (seed[0] in `data.js`): customer Sarah Chen, home visit, 88 Leonard St(reet), 4B; appointment **Sun, Jul 12 · 7:00 PM** ("Tonight" on T01/T03B/M1 — the fiction's today is Jul 12); need-by **Fri, Jul 17**. Money (round 7 model): pre-appointment alterations **$200** ($120 Hem + $80 Sleeve) = payout **$200** (T01 request + active card, T02, T03); post-appointment **$360** (added $80 Sleeve on garment 1 + third $80 Suit Jacket, per the 06B fiction) = payout **$360** (T04–T08). No commission; the tailor never sees the customer's $25 visitation fee or totals. T05 mirrors 06B (card 1 shows the added Sleeve + $200 in `semantic/info`); T06 mirrors 04D (cards itemize $120/$80/$80, totals $360-based). T08's id `TLY-2026-4417` = user 08's order number; payout deposited Mon, Jul 20. TM1 is the user M1 thread from Marco's side (sides flipped, "Sarah" for "Kevin", 16px bubbles).
- The 9 unused **T/ draft components were deleted** (Kevin: "retire drafts"); the TAILOR — T/ COMPONENTS section (`210:647`) keeps the live ones: Tailor - Garment Card, Active Job Card, T2/ rows + Chat Bubble, Selector, User Summary. No suggest-time, shop-setup, or onboarding screens for now (onboarding will own setup).
- Offset normalized to **128**; all spacing snapped to grid and bound to `space/*`; off-scale text fixed (M1 name/bubbles 16, dividers + "REOPENED SLOT" 12 t-caps, T03B note 16; Active Job Card master name 16 / "Payout" 12).
- Quirks kept deliberately: **Leo Von** active job ($102, SEP 2 badge, "Pickup: Sept 1, 2PM") is filler outside Marco's order; **T07 is the pickup branch** (07A-style: Sarah picks up Fri Jul 17 · 3:00 PM at Marco's 1025 Broadway) even though user 08 records $20 delivery; T01 shows Sarah as both new request and confirmed active job (same design fiction as user 01's slices).
- The **user refs were re-exported and `refs:check` passed** after the shared-master touches (Active Job Card sizes — tailor-only; Tailor Summary Card gap bound to space/16 — value-identical; Appt_View hidden Service Row made visible in master, hidden per-instance where unused).
- Named version "tailor build start" could NOT be saved via API — Kevin saves it manually in Figma.

## v3

`taily-prototype-v3.html` is the archived v3 prototype and the behaviour reference. `js/data.js` and `js/state.js` are ported from it. Do not port v3 CSS or markup — visuals are rebuilt from Figma.

## UX-LOOP (Sep 2026) — director-review rounds, both flows

`UX-LOOP.md` is the contract, Kevin's end state, the Figma cadence (Figma written on rounds 3/6/9 + a final sync; between syncs a "Figma sync pending" ledger) and the per-round log. Round 1 (Figma-first) landed:

- **Mirror model.** One appointment, two personas: seed[0] and every `requestTailor()` entry carry `mine: true`; the tailor side resolves Sarah's job by `a.mine` (falling back to `state.lastCancelled`, stashed by `cancelAppointment()`), never by index. `a.garments` + `a.totals` are the shared FINAL order: the tailor's T05 Send (`writeFinalOrder()`) writes the reviewed draft into them and stamps `revisedAt`; the user-side demo `draftFinalOrder()` (run by 03.2's Confirm) does the same with `SEED_FINAL_ORDER` and is a no-op once `revisedAt` is set; `approveOrder()` only stamps `approvedAt`. `markReady()` / `deliver()` stamp `readyAt` / `deliveredAt`; `chooseFulfilment()` records the handoff. The seed's own `garments/totals` are the BOOKED $200 order.
- **Fiction now consistent both sides:** HOME visit at 88 Leonard St, 4B (`where:'home'`, card titles "3 Items Total - Home Visit:"); appointment "Sun, Jul 12 · 7:00 PM" (Jul 12 2026 is a Sunday — the "Fri, Jul 12" rows were corrected in Figma); need-by "Fri, Jul 17"; ready "Thu, Jul 16"; deposit `-$20` on every 03 variant; one date grammar via `fmtWhen/fmtDay` (data.js); `money()` prints whole dollars or two decimals. The deposit is a HOLD until the tailor confirms ("held" / "released", nothing charged on a cancelled request). Cancelled requests / declined orders open 03/Cancelled with a Cancelled pill and no fee rows (the frame draws the confirmed-cancellation fixture).
- **Demo affordances added:** 03/Confirmed tailor-card tap = "the day before arrives" → 03/Reminder; T06 Mark Ready while awaiting approval = "Demo: Sarah approved".
- **Live-only states (deep links still render each frame's fixture via `isFixture()` / `!window.__tailyNavigated`):** 03/Tailoring status hero + primary CTA + `View final order ›` link row; appointment-card metas per status + "Change Pickup / Delivery"; need-by validation (`.filter-pill--error`, Request Tailor toasts until valid); custom pickup wheel bounded ready→need-by, 9 AM–6 PM; 09 partitions Current/Past by status; stored reviews (`a.review`); T03 pre-visit view ("Upcoming visit" + Start Appointment → T04; "Booking Confirmed!" only right after Accept); T06 status line + per-status CTA; T07 follows `a.fulfilment`; T04 edits a draft (`state.tailorUi.draft`, `wireOrderEditor()` mirrors 02's selectors; `+ Additional Garment` and the note textarea are live-only); T03A has no default reason live; T01's request timer ticks from `tailorUi.expiresAt` (first render "1H 24M"); T01 renders a Cancelled card → T03B.
- **Chat:** messages carry absolute authors (`tailor` / `customer`), sides derive per persona, greeting uses a `{name}` token; header = `fmtWhen(a.when) · a.visit` (M1 frame edited to match).
- **Router mirrors browser history** (`pushState` keeps `?screen=`; `popstate` closes an open overlay first, then `back()`).
- **Persona toggle** (`.stage-caption`) is fixed bottom-right and hidden until the first input (`html.has-input`) so element screenshots never include it.
- Figma edits made in round 1 are itemised in UX-LOOP.md (02 + sheet backdrops, 01/01a/09 titles, 03 rows, popup backdrops, 06.1 sub, T03/T04 avatar "SC", Appt_View ✕ hidden on T05/T06, T07/T08 chevrons, T08 payout copy, M1 header). Refs re-exported; baselines re-accepted.

### UX-LOOP round 2 (code-only; Figma sync ledger applied in round 3)

- **Mirror model v2.** The tailor side is a LIST: `jobs(s)` = every `mine` appointment in `state.upcoming` plus terminal `mine` entries in `state.past`; `primaryJob(s)` = soonest non-terminal; `state.tailorUi.current` (set by whichever T01 card was tapped) drives T02–T08 and the tailor chat; per-appointment tailor state lives on `a.tailor = { requestHandled, expiresAt, draft, declineReason, justAccepted }`. T01 renders one request card per `searching` job (own timer), one job card per open job, then Cancelled / Withdrawn / Expired rows below the Leo Von filler. The frame's "seed as request AND confirmed job" composite renders only on the deep-link fixture.
- **Explicit-target transitions.** `step(from, to, extra, a = apptEntry())` returns a boolean; `tailorAccepts / completeAppointment / approveOrder / markReady / deliver / chooseFulfilment(method, window, date, a) / declineAppointment / cancelAppointment` all take the appointment and return success — tailor screens always pass their job and check the result before toasting.
- **Terminal → past.** `cancelAppointment`, `declineAppointment`, `expireAppointment(a)`, `tailorCancels(a, 'cant-make-it' | 'no-show')` stamp `status / cancelledBy (customer | tailor | none) / reason / cancelledAt / wasRequested`, MOVE the entry to the front of `state.past` (09 lists it under Past with its card variant; 03/Cancelled reachable) and set `state.lastCancelled` to the same object. No charge on any of them (hold released) — fee policy is Kevin's call.
- **Branches built code-first (End state):** proposed time (`proposeTime(a, when)` while searching → `a.proposed`; T03A "Schedule conflict" → `Suggest Another Time` → 02.1 wheel (7 days) → T01 "Time proposed · … — waiting for Sarah" + Withdraw; 03/Requested `new-times` hero + `Accept New Time` (`acceptProposedTime(when, a)`) / `Keep Looking` (`declineProposedTime(a)` → "Sarah kept her original time")); request expiry (T01 tick/first render runs `expireAppointment` on lapsed searching jobs → Expired row, T02 expired view; customer card "Request expired · no tailor accepted" → 03/Cancelled "Request expired" + `Send Request Again`); tailor cancel / no-show (T03 pre-visit `Can't make it` → modal `t03.1-cant-make-it` → `tailorCancels` → T03B per reason; customer 03/Cancelled "Marco had to cancel" / "We missed you at …" + `Find Another Tailor`); request changes (04.1 Sounds Good → `requestChanges(a)` + opens chat with a canned bubble via `a.pendingChatSeed`; T06 line + `Message Sarah`, T01 "Sarah has questions"; approval clears).
- **Order integrity.** Garments carry stable `id`s (`addGarment`, seeds `g1–g6`, `SEED_FINAL_ORDER` `g1,g2,g7`); `orderMarks` matches by id and returns `removed`; `writeFinalOrder` / `draftFinalOrder` store `a.removed`, stash `a.booked`, and call `refreshItemSummary(a)` (cards derive title/lines from `a.garments` once `revisedAt` is set; the seed keeps its frame copy until revised). T05 and the customer's 04 render "Removed at the visit — …".
- **Dates.** `handoffWindows(a)` derives the two handoff days from `readyAt` → `needBy` (seed still Thu Jul 16 / Fri Jul 17; ONE day when `readyAt` is the need-by day); `fulfilment.window` is stored dated ("Fri, Jul 17 · 4–6 PM"); 03/Tailoring's note reads `All details confirmed on ${fmtDay(a.when)}` live. `SEED_PAST` carries coherent facts (Sep 2 pickup, need-by Sep 4, deposit 8/28/26, one name).
- **Overlay lifecycle.** `modalOverlay` / `sheetOverlay` close instantly when their holder is disconnected; a previous `overflow: hidden` is never captured; `render()` calls `closeOverlay({ instant: true })` before replacing `#screen` and clears `.toast` when the persona changes. 03.2 / 05.1 / 03.1 call `close()` before navigating.
- **Live-only states added** (deep links keep fixtures): 03/Tailoring tailoring-state CTA bar (`Message Marco` primary); 02 photo "+" tile adds a placeholder photo; T02 "Accepted" (inert) / "Request Expired"; T07 waiting-for-handoff state (`Message Sarah` primary, secondary `Mark Picked Up` = demo "Sarah chose pickup now"); T06 per-status lines; T03B copy from state ("Request withdrawn." for a withdrawn request).
- **Demo affordances added:** 03/Requested hero-pill tap = Marco proposes the next day 11:00 AM; 03/Requested "Tailors have up to 2 hours…" line tap = time passes; T01 timer-strip tap = time passes; T07 waiting `Mark Picked Up` = Sarah chose pickup now.
- **Harness.** `scripts/clickthrough-sync.mjs` (cross-persona, in `npm run check`) is the End state's synchronization proof; user click-through ~85 assertions, tailor 153.

### UX-LOOP round 3 (Figma sync)

- **Every round-2 live-only state now has a sibling Figma frame + a fixture route**, placed at the right end of its base's stage row and named per the numbering scheme: `03 - Order Status / Expired · Declined · Tailor Cancelled · No-Show · New Time`, `04 - Review & Approve / Removed`, `05.1 - Window Confirmed / Dated`, `09 - Bookings / Closed Cards`, `T01 - Home / Closed Rows`, `T02 - Appointment Request / Accepted · Expired`, `T03 - Upcoming Visit`, `T03.1 - Can’t Make It`, `T03A - Decline Request / Suggest Time`, `T03B - Job Cancelled / By You · No-Show · Withdrawn`, `T05 - Confirm Final Pricing / Removed`, `T06 - Appointment Status / Sarah Has Questions`, `T07 - Job Ready / Waiting`, `TM1 - Message Customer`. Each is a duplicate of its base with only that state's text / visibility changed (instances stay instances; fee rows, refund card, spare CTAs and cards are HIDDEN, not deleted). Ids → nodes in `scripts/screens.json`.
- **Fixture routes** (`js/screens/<id>.js`, one line each) call the base screen's exported view with a fixture from `js/fixtures.js` (the seed appointment pushed into that state — `APPT_EXPIRED()`, `APPT_PROPOSED()`, `FINAL_ORDER_REMOVED()` …) or a `mode` flag (`viewRequest(s, 'accepted')`, `viewAccepted(s, 'upcoming')`, `viewDecline(s, 'suggest')`). The base screens' own deep links render exactly as before. Per-route `min-height` rules in `screens.css` / `tailor.css` match the frames' hugged heights (the 03 variants are shorter than the phone, like the tailor frames). The persona gate keys on `/^t\d/` so `t10-messages` renders as Marco.
- **Text-only syncs:** 03/Tailoring's note reads "All details confirmed on Sun, Jul 12." and its CTA bar leads with `Message Marco` (Add to Calendar gone — the appointment already happened); the 03.3 backdrop copy carries the same edits; the code's harness/live split for both is gone.
- **Known gaps (Kevin):** the Appointment Card `Status=Requested` variant has no Actions slot, so `09 - Bookings / Closed Cards` cannot draw the `Review Time` small CTA the code renders under the proposed-time card (baseline 8.08 is that 40px row); the T03A "Suggest another time" wheel is an overlay with no frame; the `Ready` card's dated window and 03/Tailoring's scheduled / ready / delivered heroes stay live-only (Kevin: "Ready is a live state of the Tailoring frame"). The round-2 fee-policy default (no charge on tailor cancel / no-show) is drawn in the new 03 frames as "Nothing was charged".

### UX-LOOP round 3 fixes (after the sync)

- Proposals are bounded by the need-by DAY on both sides: `proposalDays(a)` feeds the T03A wheel and the customer demo; `proposeTime` returns false past it (T03A toasts); the `new-times` hero names the need-by. `declineProposedTime(a, by)` records who declined (`{ when, by }`); `expireAppointment` keeps `a.lapsedProposal` so both sides can say the proposal went unanswered.
- `requestTailor()` clears `state.garments` and the Home selection once the appointment owns them, stamps `a.orderId` (`nextOrderId()`, seed `TLY-2026-4417`); 03/Requested renders the appointment (form only on the harness deep link).
- Home (01): today's terminal outcome (`state.lastCancelled`, mine) renders once above the live card; live card = first non-delivered, non-terminal entry; delivered-only fallback under "Recent Appointment". Every 03 view guards a terminal `currentAppt` → 03/Cancelled, and terminal / accept navigations use `replace`.
- Deposit wording: confirmed bookings ended by the tailor say the deposit is refunded (customer 03/Cancelled + T03B); "hold released" only for never-confirmed requests. `payoutDate(a)` = handoff day + 4 → weekday (seed still Mon, Jul 20).
- T01 sections: New Requests / Active Jobs / Done today (closed rows `.job-card--closed`, no payout); `primaryJob()` never picks a closed job. The Can't-make-it modal carries a consequence line, `Cancel Job` / `Mark No-show` labels, and a no-show row gated on the visit time (seed's Jul 12 counts as today).
- Harness: sync click-through 796, tailor 174; the round 3 fixture frames were edited in place for the new copy (T01 Closed Rows "Done today", T03.1 line, T03B and 03 variant bodies).

### UX-LOOP round 4

- Proposals are bounded by the need-by day AND time: `proposalDays` / `proposalHours` / `proposalMins` (shared `STUDIO_HOURS` 9 AM–6 PM) feed the wheel, which re-rows hours as the day settles; `proposalDays` may return `[]` (T03A then shows the no-slot line and keeps Decline). `proposeTime` compares the datetime on the need-by day.
- The 03 family guard also redirects a post-appointment entry (03/Confirmed, 03/Reminder → 03/Tailoring, `replace`); 03.2 Confirm and the Confirmed→Reminder demo use `replace`; `cancelAppointment()` refuses post-appointment statuses. `a.payMethod` is stamped per booking and read by 03/Cancelled. T05 Send / T06 Mark Ready on a closed job return to T01 with "This job is no longer on your calendar".
- Harness: click-through 117, tailor 199, sync 807.

### UX-LOOP round 6 — Kevin's decisions (Figma round)

- **Fee policy (Kevin, round 6 — SUPERSEDED by round 7):** tailor cancels → deposit refunded; customer cancels within 12 hours of the visit, or no-shows → deposit kept. `cancelAppointment` / `tailorCancels` stamp `a.refund` and `a.depositKept` (`withinHours(when, 12)` in data.js); copy on 03.1, 03/Cancelled, T03.1 and T03B follows them. "Fee" here is the customer's 10% deposit.
- **Reschedule (Kevin):** = cancel + resubmit the same job at the same time for a new tailor. `rescheduleAppointment(a)` cancels (12-hour rule), copies items + time + need-by + visit type into the booking form and lands on 02; the customer never chooses the tailor.
- **Tailor anonymity (Kevin):** no tailor name until a tailor accepts. `requestTailor()` sets `name/initials/tailorId = null, matching = true`; `tailorAccepts` / `acceptProposedTime` assign Marco. Use `tailorName(a)` ("Matching you with a tailor") / `tailorInitials(a)` ("✂") wherever the customer side prints the tailor.
- **Support chat:** T04 "Contact Taily Support" opens `10-messages` on `state.chats.support` (authors `support` / `tailor`, canned reply). `openChat(kind)` in tailor-components picks the thread.
- **T03A "Other"** reveals a textarea → `a.declineNote`; fixture route `t03a-other` (frame 629:4443). **T02** shows visit type · distance and a Subtotal row. **Done today** has a `Clear` link (`tailorUi.clearedClosed`). **05A/05B** CTAs carry the dated window and read `Switch to Delivery` / `Switch to Pickup` (frames synced). The "Tailors have up to 2 hours…" line is in the 03/Requested frame (16px). Six older frames now write `-$20` / `7:00 PM` (parity ALLOWs gone). Flow chart `263:6457` has the approval gate and the suggest-time branch.
- Frames still taller than 844: 03 Tailor Cancelled / No-Show / New Time (hug their content).

### UX-LOOP round 7 — money model v2 (Kevin; code-first, Figma money sync pending)

**Supersedes every earlier deposit / 10% note above.** No deposit and no commission exist anywhere.
- **Visitation fee** by booked item count (quantities summed): 1–4 $25, 5–10 $50, 11+ $100; the $50/$100 tiers show "Helps cover transportation for larger appointments." It is Taily's only revenue and it IS the customer's deposit: held at request (`a.feeHeld`), charged when a tailor accepts (`a.feeChargedOn`), refundable until the customer confirms at the 24-hour reminder (`confirmAppointment(a)` → `confirmedAt`, `feeLocked`), then non-refundable (no-show included). Unconfirmed 12 h before the visit → `autoCancelUnconfirmed(a)` (reason `unconfirmed`, refund). Tailor cancel always refunds. Re-tiered when the tailor adds items (`visitFeeAdded`); removing items never lowers a charged fee.
- **`a.totals = { alterations, items, visitFee, visitFeeCharged, visitFeeAdded, delivery, total }`** (`subtotal` is a deprecated alias of `alterations`). `apptTotals(garments, base)`, `visitFee(n)`, `visitFeeNote(n)`, `payout(garments)` live in data.js. Customer rows: Alterations / Visitation fee — paid / (Additional visitation fee) / (Delivery $20) / Total / Due at handoff; alterations (+ delivery) are charged at handoff.
- **Tailor sees only earnings:** garment cards carry the alteration prices (the breakdown) and one `Your payout` row = 100% of them; `a.tailor.acceptedPayout` is stamped at Accept and only moves with the final order; T05 shows `Payout $200 → $360 (+$160)` before Send. Tailor screens never print the customer's total, the visitation fee, delivery or any margin (the tailor click-through sweeps the DOM for those words). Refund lines name "her visitation fee" without an amount.
- **Demo affordance:** tapping 03/Reminder's title = "12 hours pass without confirming" → auto-cancel. 03.2's Confirm locks the fee and completes the visit in one step (demo compression), so the locked pill / 03.1 row are exercised by the harness through the substrate.
- **Seed:** $200 alterations + $25 fee = $225; after the visit $360 → $385 (+$20 delivery = $405); payout $200 → $360. The frames still draw the old deposit / 10% rows: 36 baselines are accepted `pending-figma` and `text-parity.mjs` carries `R7_*` ALLOW groups until the dedicated money sync.

- **Round 7 fixes:** 03/Reminder carries a "Before you confirm" callout (`.fee-callout`, prepare-card style) above Confirm and 03.2 restates the lock; `orderRows(t, { tier: true })` captions the added fee with count + tier and `feeTierNote(t)` explains it (04, 03/Tailoring); 02 fee card shows the alterations estimate; 03/Cancelled has no Total row (`orderRows(..., { total: false })`); tailor T03.1 / T03B never mention the customer's fee; `jobCard({ pending })` → "Payout · pending" while awaiting approval; `payoutRows(v, { offered: true })` on the expired T02; T05 live sub names the draft payout.

### UX-LOOP round 8 — Figma money sync (full parity) + no-show compensation

- **Figma now matches the money model.** Every money row on both pages reads the build's copy; no 10% / deposit / Taily fee / Balance text is visible anywhere in the file (old fee rows are hidden, not deleted). The `R7_*` parity ALLOW groups are empty. New frames: `03 - Order Status / Reminder Locked` (644:5949 → `03-status-reminder-locked`), `/ Confirmed Locked` (644:6051 → `03-status-confirmed-locked`), `/ Unconfirmed` (644:6149 → `03-status-unconfirmed`), `04 - Review & Approve / Re-tiered` (644:6244 → `04-review-approve-retiered`, fixture `FINAL_ORDER_RETIERED()` = seed final order + `g8` 2× Pants / Jeans Hem $240 → 5 items, $600 / $25 / +$25 / $650 / $625 due). The Appointment Card `Status=Requested` variant has an Actions row (`Review Time`).
- **No-show compensation (Kevin):** `noShowComp(fee)` = `fee >= 50 ? fee / 2 : 20`; stamped as `a.noShowComp` by `tailorCancels(a, 'no-show')`. Tailor side only: T02 protection row (`payoutRows(v, { protection })`, not on Expired), T03B "You'll receive $X for the trip.", Done today `No-show · $X`. The tailor never sees the fee; the customer's refund / kept logic is unchanged.
- 03 variant frames taller than 844 stay hugging (content-driven); `min-height` per route in `screens.css` tracks the frame heights.

### UX-LOOP round 9 — Kevin's 02 / 03-Confirmed edits (user flow only; Figma synced)

- **02 pills start empty:** `APPT_DEFAULT.when / needBy = null` → both Filter Pills read "Select Time" (`PILL_EMPTY`); `requestBlocker()` keeps Reserve inert (pill error + toast "Select a requested time" / "Select a need-by time", then the need-by order rule). `fmtPill()` (data.js) is the pills' one grammar ("Jul 12, 7:00 PM", day alone without a time) — `rescheduleAppointment` and the re-request seeds write through it. `requestTailor()` tolerates a null `when`.
- **02 fee card = garment-card chassis** (`.fee-card` in screens.css: 68px `.fee-card__chip` price column left, `.fee-card__content` right; "Visitation fee · 2 items" SemiBold 16 ink) and lives INSIDE `.cta-bar` above the CTA (as the frame draws it). CTA copy **"Reserve Appt · $25"** (tier follows the count). 02's screen min-height is 1012 (the frame). Frames 02 + 02.1–02.4 rebuilt in place (Fee Card nodes 640:2913…2925 now HORIZONTAL with `Price Chip` / `Content`).
- **03/Confirmed photo tiles open the viewer:** `wireBookingPhotos(root)` (03.3 module) → `openPhotoViewer({ booking: true, count, active })` — same panel, title "<garment> — Your photos", sub "Added when you booked", no legend, one thumb per photo. Only 03/Confirmed (+ Locked) is wired; other pre-visit tiles stay inert. Sibling frame `03.3 - Photo Viewer / Booking` 654:5774 → route `03.3-photo-viewer-booking` (03/Confirmed backdrop).
- Kevin typed "Reserve Appt • $25"; built with the app's "·" (flagged).

### Test flows menu (Sep 13 2026, Kevin) — prototype chrome, not a Figma frame

- The bottom-right "View as Tailor" pill is now **Test flows** (`js/flow-menu.js` + `css/flow-menu.css`, mounted into `.stage-caption` by app.js after the screen modules load; still outside `.screen` and hidden until the first input). The panel lists every step of both flows by stage, on Customer flow / Tailor flow tabs; the footer keeps the persona flip (`#persona-toggle`, renders the other side's home WITHOUT resetting state).
- **`js/flows.js` is the registry** (73 entries). Each entry = `{ key, code, title, note, screen, setup(a), click? }`. Choosing one runs `resetDemo()` (seed lists, no ui / chats / tailorUi flags), drives the seed appointment (`mine`, Jul 12) through the REAL transitions (`asRequest` / `accepted` / `visited(order)` via `writeFinalOrder` with the `DRAFTS` modified · unchanged · removed · retiered / `approved` / `ready` / `scheduled` / `delivered`, plus the terminal transitions), points `state.currentAppt` + `tailorUi.current` at it, renders the screen live with a fresh history (`render(id, { fresh: true })`), then presses the screen's real buttons for overlay entries. **Never hand-write fixture fields onto the appointment in a setup** — compose the transitions so both personas stay consistent.
- `?flow=<key>` boots straight into a step (the jump also writes it into the URL); `?screen=` deep links are unchanged, so the diff harness never sees the menu.
- **Adding a screen or state = add an entry to `FLOWS` and an `EXPECT` snippet in `scripts/clickthrough-flows.mjs`** (in `npm run check`; clicks every entry through the UI and asserts screen, persona, overlay, state text, no errors; `--dump` prints what each entry lands on). `clickthrough-sync.mjs`'s `flip()` opens the menu before clicking `#persona-toggle`.

### UX-LOOP round 10 — Kevin's 02 / approve-flow / 03-Requested / tailor-resend edits (Figma synced)

- **`statusScreen(a)` (state.js) is THE status → screen map**: `apptTarget`, the 03/Confirmed · Reminder · Tailoring guards, 04's stale guard and 03.2's Confirm use it. **While the final order awaits approval, 04 Review & Approve is the screen** (Kevin: one approve screen) — `04-review-approve-modified` when the visit changed the booking, else `04-review-approve`; 03/Tailoring only from `tailoring` on. 03/Tailoring's "Review Final Order" CTA and "View final order ›" row are gone.
- **"View All Appointments" is gone from every 03 and 04 screen** (code + frames: CTA instances hidden; 03/Summary's whole CTA Bar hidden). 03 route `min-height`s follow the new frame heights (Confirmed / Locked 1098, Tailoring 1152.25, Summary 1089.5).
- **02 is the new frame 657:4875** (old 277:2676 deleted by Kevin): editable cards as flat rows (`garmentCard({ flat })`, `.garment-card--flat`) inside `.garments-card`, then `orderRows(totals, { est: true, feeDesc: 'Visitation fee - Due Today' })` + the paid-at-handoff note (+ tier note on $50 / $100), "+ Additional Garment" below the card, no fee card. Sheet backdrops 02.1–02.4 re-synced via API. Known frame quirks (raised): card 2 has no service line; the Additional Service selector is a detached 155×29 frame (master is 159×32) — built per the master, the 7.64 baseline carries both.
- **03/Requested rows:** `✂ 2 items · $200.00+ est.` and `🏠 $25 visitation fee` (fee row on its own; 03/New Time split the same way).
- **Tailor edit-and-resend:** T06 shows **Edit Details** while awaiting approval → `reopenDraft(a)` (draft from the SENT order, ids kept) → T04 → T05 in resend mode ("Updated after sending · Payout $X", **Resend to Sarah for Approval**) → `resendFinalOrder(a, draft)`: writeFinalOrder, `a.resentAt` / `resendCount`, clears `changesRequestedAt`, queues `a.pendingTailorNote` (10-messages pushes it as a tailor bubble). Customer 04 adds "Marco updated the order on <day>. Changes are highlighted below." Frames: T06 / Sarah Has Questions gained Edit Details; new `T06 - Appointment Status / Awaiting Approval` (666:4837 → `t06-awaiting`), `T05 - Confirm Final Pricing / Resend` (666:4979 → `t05-resend`).
- The tailor Active Job Card master's items label was re-created by Kevin ("2 Suit Jackets" default, 659:5821); T06 instances carry the "3 Suit Jackets" override. Flows menu: `c-resent`, `t-edit-details`, `t-resend`, `t-status-resent` (`c-awaiting` retired).
